# ==============================================================================
# ADDMAI Runtime Requirements (Production & Docker)
# ==============================================================================
# Note: pyproject.toml is the authoritative project metadata source.
# This file is maintained for container build compatibility (apps/api/Dockerfile)
# and standard pip deployment environments.

# --- Core Stage 1 Foundation Dependencies ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0

# --- Reserved Dependencies for Upcoming Stages ---
# Stage 2: S3 Object Storage & Media Ingestion
aioboto3>=12.3.0
python-multipart>=0.0.9

# Stage 3: PostgreSQL Database & Migrations
sqlalchemy[asyncio]>=2.0.28
asyncpg>=0.29.0

# Stage 8: Task Broker & Redis Cache
redis>=5.0.2
